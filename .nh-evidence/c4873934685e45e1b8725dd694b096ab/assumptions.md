# Assumptions

_Harness-captured record for task `c4873934`, commit `24c112012f3ec916763e0722f32289eb3a2ee1bb` — not model-authored: no_human wrote this file from the intake step's recorded questions and assumptions. It records what the gate produced; it is not a verdict of the model that wrote the code._

<details><summary>⚠️ 4 assumptions made on your behalf — verify at review</summary>

- **Q:** What are the exact intermediate steps to instrument on the onboarding path (e.g., onboarding_screen_viewed, repo_selected, repo_invalid, auth_check_started/succeeded/failed, task_create_failed), and what are their closed value vocabularies? **A:** Add these new events to _ALLOWED_EVENTS (not _LAMBDA_EVENTS), mirroring the existing pattern (name -> frozenset of allowed prop names, plus enum value-checks like TASK_END_OUTCOMES): onboarding_screen_viewed{step, environment} with step enum matching the onboarding surfaces (e.g. {'welcome','repo_select','ai_config'}); repo_selected{environment} (no path/content, just occurrence); repo_invalid{rea _(repo-evidence)_
- **Q:** How should credential validity be checked (e.g., a real API call/ping to the AI provider) to distinguish 'present but invalid' from 'valid', and is making that live validation call at onboarding time acceptable (network cost, provider rate limits, added latency)? **A:** Yes, a live validation call (a minimal, cheap ping/list-models style call to the configured AI provider) is the only way to distinguish 'present but invalid' from 'valid' per the acceptance criteria ('observable, not merely whether one is present'), and is acceptable since it happens once at onboarding time (not per-task), not in a hot loop; result is reported only as auth_check_succeeded/auth_che _(assumption)_
- **Q:** For the ph-no-capture dead-click blind spots on Board.jsx:364 and App.jsx:598, should these be replaced/supplemented with an explicit bucketed telemetry event instead, or is fixing dead-click capture out of scope for this task? **A:** Out of scope for this task: acceptance criteria is scoped to onboarding-path instrumentation and auth-validity observability; the ph-no-capture dead-click blind spots on Board.jsx:364 and App.jsx:598 are corroborating pain signals cited as context, not listed in the acceptance criteria, and fixing them would expand the diff into unrelated capture-config changes on components outside the onboarding _(repo-evidence)_
- **Q:** Should client-side $exception capture be turned on as part of this task, given it currently appears to be off? **A:** Out of scope: enabling client-side $exception capture is a PostHog SDK config change orthogonal to the closed-allowlist onboarding/auth events described in the acceptance criteria, and turning it on risks capturing uncontrolled exception messages/stack content, which conflicts with the task's explicit 'no user or repository content, only closed vocabularies' discipline unless separately scrubbed — _(assumption)_

</details>

