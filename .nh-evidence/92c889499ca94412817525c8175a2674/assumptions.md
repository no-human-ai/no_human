# Assumptions

_Harness-captured record for task `92c88949`, commit `74de86714eea7d37633cd2f5de4cc7f9a119d359` — not model-authored: no_human wrote this file from the intake step's recorded questions and assumptions. It records what the gate produced; it is not a verdict of the model that wrote the code._

<details><summary>⚠️ 4 assumptions made on your behalf — verify at review</summary>

- **Q:** Should the Linux acceptance driver attempt to run a task on the no_human service, or is verifying board/Settings/Stats navigation sufficient for acceptance? **A:** Verifying board/Settings/Stats navigation is sufficient for acceptance. Task execution is not required in this driver. The runner's shape-valid-only dummy token cannot authenticate real API calls, so task-running would require either hollow assertions (task not actually running) or complex partial-success logic. The goal is to verify the UI surfaces are reachable post-onboarding; service functiona _(assumption)_
- **Q:** When progressing through the onboarding wizard (WELCOME → EMAIL → REPOSITORIES → PROJECTS → INTEGRATIONS → COMMUNITY → LAUNCH), should each wizard screen be individually asserted as a discrete step, or is asserting only that the final board is reached sufficient? **A:** Assert only that the final board is reached, not each individual wizard screen. The current defect is reaching step 1 instead of the board; that is the critical gate. Asserting all 7 wizard screens individually would add fragile maintenance burden if the wizard UI changes. The driver should verify entry into the wizard and successful exit to the board with a single assertion at the board destinati _(assumption)_
- **Q:** If task execution is in scope: given the runner's shape-valid-only dummy token, what behavior should the driver assert? Should it assert task creation succeeds but API calls fail, or assert failure at a specific earlier stage? **A:** Not in scope for this driver. The task's direction section does not mandate task execution; it lists it as context for what a human walk covers, not what this automated driver must cover. The constraint that the dummy token precludes authentic task completion argues against including it. A separate acceptance test with real credentials should cover task execution. _(assumption)_
- **Q:** To verify that screenshot filenames describe actual rendered content (not repeating the 02-board.png defect), should the test validate this via programmatic image analysis, DOM/page-title inspection, or another approach? **A:** Use DOM/page-title inspection before taking the screenshot. Before writing a screenshot file, query the browser for board-specific elements (e.g., task list container, board header text, or page-title content) and assert they are present. Only if the assertion passes does the driver write the screenshot. This avoids image analysis complexity, is faster, and directly tests that the correct page is _(assumption)_

</details>

