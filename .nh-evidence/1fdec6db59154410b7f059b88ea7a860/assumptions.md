# Assumptions

_Harness-captured record for task `1fdec6db`, commit `15db8b7aeb353cb14894e4252370c3d7bc54a640` — not model-authored: no_human wrote this file from the intake step's recorded questions and assumptions. It records what the gate produced; it is not a verdict of the model that wrote the code._

<details><summary>⚠️ 6 assumptions made on your behalf — verify at review</summary>

- **Q:** Which file or module contains the implementation of the 60,000-character diff budget check (the "nh gate")? **A:** The 60,000-character diff budget check is most likely implemented in a pre-merge CI gate—either a Python script in the scripts/ directory (given the presence of scripts/check_release_manifest.py) or as a GitHub Actions workflow step that measures total diff size before gating other checks. _(assumption)_
- **Q:** Where is the "tamper guard" mechanism implemented that compares hashes without reading diffs, and is it separate from or integrated with the gate code? **A:** The tamper guard is the 'File inventory CI job' explicitly mentioned in the task description. It is separate from the diff budget gate: the gate measures character count to prevent truncated-diff review, while the inventory job compares SHA256 hashes and never reads the diff content. Both may reference the same RELEASE_MANIFEST.txt but through entirely independent mechanisms. _(assumption)_
- **Q:** Besides RELEASE_MANIFEST.txt, are there other generated, hash-verified files in the repo that should be included on the initial allow-list? **A:** Without repo inspection, likely generated and hash-verified candidates beyond RELEASE_MANIFEST.txt could include lock files (package-lock.json, poetry.lock), generated protocol buffers, or build artifact manifests. The allow-list should only include files confirmed to be auto-generated and pinned by hash in an inventory mechanism. _(assumption)_
- **Q:** How should the generated-paths allow-list be defined and maintained: as a code constant, config file, separate manifest, or another approach? **A:** Define the allow-list as a frozenset or tuple of explicit path strings (e.g., `GENERATED_PATHS_EXCLUDED_FROM_BUDGET = frozenset(['RELEASE_MANIFEST.txt'])`) in the Python module that implements the diff budget gate. This co-locates the allow-list with the gate logic, makes it reviewable in diffs, and avoids pattern-based matching which could silently allow new generated files. _(assumption)_
- **Q:** Should the gate's character count output explicitly show the exclusion (e.g., "X characters, excluding Y from manifest"), or just display the budgeted count which will be automatically lower? **A:** The gate should explicitly show the exclusion in its output, e.g., 'Diff budget: 52,695 characters (60,000 limit; 7,412 characters from RELEASE_MANIFEST.txt excluded)'. This transparency prevents users from misinterpreting the lower count and provides an audit trail for future maintenance of the allow-list. _(assumption)_
- **Q:** For the manifest change notification, what should "N rows" represent: all lines that differ from the prior version, or the count of lines added/removed/modified? **A:** 'N rows' should represent the total count of manifest entries that differ from the previous version (i.e., rows added, removed, or modified combined into a single count). This gives a sense of manifest churn in one number and avoids requiring users to calculate or read individual diffs. _(assumption)_

</details>

