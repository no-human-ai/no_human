# Assumptions

_Harness-captured record for task `e46707ae`, commit `19741622aed4d90f6fe18f4cae8ec9112c5e56e8` — not model-authored: no_human wrote this file from the intake step's recorded questions and assumptions. It records what the gate produced; it is not a verdict of the model that wrote the code._

> ⚠️ **Unresolved:** You've hit your monthly spend limit · raise it at claude.ai/settings/usage?from=cc_cli_limit_message ('default' subscription)

<details><summary>⚠️ 13 assumptions made on your behalf — verify at review</summary>

- **Q:** Does the merge-ready verdict record already store the trunk sha (or merge-base sha) it was measured against, or must that field be added? **A:** It does not store it today. GateFacts in src/no_human/core/merge_policy.py:108-123 has fields like tests_ran, tests_passed, tests_failed, verifiers_ran, review_passed, etc., but no trunk_sha/merge_base_sha field, and grepping merge_policy.py for 'merge_base'/'base_sha'/'trunk' returns zero matches. So recording the trunk sha (or merge-base) at measurement time is the first half of the work; the co _(assumption)_
- **Q:** Where should the staleness check live — inside `nh approve`'s landing path only, or also in whatever surface produces the `rules 6/6` listing (and any other 'merge-ready' reporting surface)? **A:** It must live in the shared evaluation path used by both surfaces, not just nh approve's landing path: PolicyVerdict/_evaluate in src/no_human/core/merge_policy.py (around line 452) is the common evaluator that both the 'rules 6/6' listing and nh approve's landing check consume (tests/test_merge_policy_wiring.py exists specifically to test that wiring). Adding a new Rule (e.g. 'base_not_stale') to _(assumption)_
- **Q:** What exact trunk ref should 'current tip' mean for the comparison — the local repo's `origin/main` after a fetch, or some cached/last-known trunk sha the tool already tracks? **A:** Use the tool's existing locally-known trunk-tip value (whatever the repo/tooling already treats as current trunk for other staleness checks, e.g. tests/test_base_staleness_pushed_branch.py's notion of base staleness) rather than issuing a fresh network fetch. The check must be a comparison of already-recorded/local facts (recorded trunk sha at measurement vs. current local trunk sha) with no new f _(assumption)_
- The merge-ready verdict record does not currently store the trunk sha it was measured against; recording it (as merge_base_sha, the sha of trunk's tip at test-run time) is the first half of this work, and the check is added as a new comparison rule rather than assumed pre-existing.
- 'Trunk's current tip' means the tip of the branch configured as the repo's default/base branch (origin/main here) at the moment `nh approve`/listing evaluates the candidate, fetched or read from local git state without a network CI call.
- 'Merge base is still trunk's tip' is checked via `git merge-base <branch-head> <trunk-tip>` at evaluation time and comparing that to the recorded trunk sha stored with the green verdict — i.e., staleness means recorded_trunk_sha != current trunk tip sha (a simple sha inequality, not a diff-content check), since acceptance criteria demand this be a bounded, run-free comparison.
- The new rule is added as a 7th rule (or folded into the existing `tests_ran_and_passed` rule's evaluation) reported in the same `rules N/N` surface, named something like `merge_base_current` or `tests_measured_on_current_trunk`, and its failure reason string names staleness explicitly (e.g. "tests ran against trunk sha X, current tip is Y").
- For tasks with no recorded test result: the new rule is not evaluated / is vacuously skipped, and the existing 'no result' refusal reason and behavior remain unchanged (same refusal path, not reached via the new rule).
- For tasks with a recorded failing result: the new rule is likewise not the deciding factor — the existing failure refusal fires first/independently, so behavior is unchanged; the new rule only changes outcomes for previously-green, now-stale verdicts.
- Recording the trunk sha is done at the time the test harness records `{ran, ok, passed, ...}` for a given branch head sha, by capturing `git merge-base HEAD origin/main` (or equivalent) at that moment and persisting it alongside the existing fields in the same product/verdict store (JSON record), with no schema migration needed for historical records beyond treating missing trunk-sha as 'unknown/s
- The pinning test advances trunk by creating/merging a commit on the test-double trunk ref (not a full CI run), then re-invokes the listing/approve check functions directly against stored fixtures — no real 25-minute suite execution is used in the test itself, consistent with 'costs no test run'.
- 'nh approve' and the merge-ready listing surface share a single underlying policy-evaluation function, so implementing the rule once in that shared function satisfies 'no surface offers it' without needing to touch multiple independent code paths.
- The added per-candidate cost is one `git merge-base` + sha comparison (or a stored-sha equality check if merge-base was already computed at record time), i.e., O(1) git plumbing calls per candidate, no working-tree checkout and no test execution — this is what will be stated as the cost in the PR description.

</details>

