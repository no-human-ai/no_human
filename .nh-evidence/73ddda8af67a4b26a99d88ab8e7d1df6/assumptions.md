# Assumptions

_Harness-captured record for task `73ddda8a`, commit `a29342858ceee02172e1dfe99a8b881f6e20cab0` — not model-authored: no_human wrote this file from the intake step's recorded questions and assumptions. It records what the gate produced; it is not a verdict of the model that wrote the code._

<details><summary>⚠️ 4 assumptions made on your behalf — verify at review</summary>

- **Q:** To identify that pytest ran a test session (vs. failing at import time), which pytest output indicators should we check for: (a) presence of '=== FAILURES ===' section, (b) presence of a summary line like '1 failed/passed', (c) presence of 'collected N item(s)' output, or (d) some specific combination? **A:** (c) Presence of 'collected N item(s)' output is the most reliable primary indicator that pytest executed a test session, combined with checking for the summary count line matching `_EXECUTED_RE` pattern (1 passed/failed/errors/xpassed/xfailed). The presence of either 'collected' output OR the regex-matched count line proves a session ran; their absence together proves it did not. The FAILURES sect _(assumption)_
- **Q:** Should we parse stdout and stderr separately (import errors typically on stderr at process startup, test output in stdout), or continue checking combined captured output? **A:** Continue checking combined captured output (stdout + stderr concatenated), as the current code does (line 394). Separating them would not improve detection: import-time pytest errors appear on stderr but the absence of pytest-session indicators (like 'collected' or count lines) in the combined output suffices to distinguish 'no session ran' from 'session ran but failed'. The key is presence of ses _(assumption)_
- **Q:** Should this fix detect only the literal 'No module named pytest' / 'No module named \'pytest\'' phrases, or also related pytest import failures (e.g., plugin load errors, missing dependencies)? **A:** Detect only the literal phrases 'No module named pytest' and 'No module named \'pytest\'' (unchanged from line 399). The fix is narrowly scoped to pytest's specific import error that prevents session launch, not broader import failures (plugin load errors, missing dependencies). The task specifies 'confined to the detection at repro_gate.py:399's region'. _(assumption)_
- **Q:** When all tests pass cleanly (no FAILURES section), what pytest output pattern proves a session ran? Should we check for success summary lines like 'X passed', or is another marker sufficient? **A:** When all tests pass cleanly, the pattern '1 passed' (or 'N passed') in the summary line — the same `_EXECUTED_RE` match that proves session execution for failing tests (line 429: `_EXECUTED_RE = re.compile(r"\b\d+\s+(passed|failed|errors?|xpassed|xfailed)\b")`). Passing and failing runs both emit a summary count; presence of that count proves the session executed. _(assumption)_

</details>

