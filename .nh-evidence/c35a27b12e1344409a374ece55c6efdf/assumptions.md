# Assumptions

_Harness-captured record for task `c35a27b1`, commit `4f1e0bffd8d5f564286859f235dc2d6937b107b5` — not model-authored: no_human wrote this file from the intake step's recorded questions and assumptions. It records what the gate produced; it is not a verdict of the model that wrote the code._

<details><summary>⚠️ 5 assumptions made on your behalf — verify at review</summary>

- **Q:** What constitutes a 'signal family' in the existing feasibility system, and which signals should be counted as separate families for the >=3 detection rule in multi_family? **A:** A signal family groups related complexity indicators within a single analytical dimension—e.g., description-text features (bullets, decomposition markers), requirement-count features (acceptance criteria count), time-estimate features, or structural/pattern features. Each distinct dimension counts as one family. The >=3 detection counts how many independent dimensions have ≥1 signal firing. _(assumption)_
- **Q:** What are the exact prefix keywords for the multi_family description-based trigger (just Fix/Issue/Part, or variations like Fixed/Fixes/Issues/Bug/Problem/etc.), and should matching be case-insensitive or require word boundaries? **A:** Prefix keywords: Fix, Issue, Part and their conjugations (Fixed, Fixes, Issues). Matching should be case-insensitive with word-boundary checks to avoid false matches on words like 'profitable' or 'issue-tracking'; the lead-in must appear at or near the start of a bullet/numbered item line (after punctuation/whitespace). _(assumption)_
- **Q:** What should be the exact type and structure of the 'signals' and 'reasons' tuple returned by hint_signals(task, ...), and how should feasibility.py consume and integrate this output? **A:** hint_signals(task, ...) returns a tuple (signals, reasons) where signals is a dict[str, bool] or list[dict] structurally matching compute_tier's signal output (one entry per signal), and reasons is a list[str] with human-readable explanations for each new hint-only signal that fired. feasibility.py unpacks this tuple and merges the hint-only signals into the card display separately from tier-affec _(assumption)_
- **Q:** Where in the codebase are existing feasibility configuration keys documented and read (e.g., how are other feasibility thresholds implemented), so feasibility.hint_signals_enabled can follow the same pattern? **A:** Existing feasibility config keys are likely defined in a single config module or settings class (e.g., src/no_human/config.py or within src/no_human/core/feasibility.py itself), read via a centralized config loader or dataclass. feasibility.hint_signals_enabled should be documented as a boolean key in the same location, and read at function-call time (not cached at module load) to allow runtime co _(assumption)_
- **Q:** Do 'stored measures' test fixtures already exist in the repository, or should they be generated/created for the hint test acceptance criteria? **A:** Stored measures test fixtures likely do not yet exist for this new feature and should be created by building Task objects using existing test builders (or direct construction), then serializing representative examples (quick task, bulleted description, multi-AC task, etc.) into tests/fixtures/ or inline in the test file for repeatable validation. _(assumption)_

</details>

