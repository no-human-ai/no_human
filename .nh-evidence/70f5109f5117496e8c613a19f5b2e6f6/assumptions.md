# Assumptions

_Harness-captured record for task `70f5109f`, commit `29a1a1bb6151c6ad75258fc6cded07008b42003e` — not model-authored: no_human wrote this file from the intake step's recorded questions and assumptions. It records what the gate produced; it is not a verdict of the model that wrote the code._

<details><summary>⚠️ 4 assumptions made on your behalf — verify at review</summary>

- **Q:** What return value(s) or conditions from a verifier should be treated as 'no verdict'? (e.g., None return, timeout exception, unparseable LLM response, or a specific sentinel value?) **A:** None return value from verifier judge call, or exception (timeout/network error) after exhausting the bounded retry attempts. A verifier that never returns a parseable verdict/PASS/FAIL is treated as 'no verdict'. _(assumption)_
- **Q:** Does the current verifier_results data structure already support recording no-verdict outcomes, or must the schema be extended to add a new 'no_verdict' verdict type? **A:** verifier_results schema must be extended to add a 'no_verdict' outcome type distinct from PASS/FAIL verdicts, enabling the advisory outcome to be recorded and visible in review events and operator tooling. _(assumption)_
- **Q:** Does the 'nh verifiers list' command already exist in the codebase, and if so, in which file(s) is it implemented? **A:** The 'nh verifiers list' command likely already exists as an operator CLI command; the task requires enhancement to add a per-verifier count of no-verdict outcomes alongside existing stats (pass/fail rates). _(assumption)_
- **Q:** For the performance regression requirement: what specific metrics (latency, memory, complexity) should be measured, what are the current baseline values, and what regression thresholds are acceptable? **A:** Measure latency of _run_review function with no-verdict path vs. baseline (PASS/FAIL paths); measure total review round latency; measure bounded retry loop overhead. Acceptable regression threshold: <5% latency increase on median and p95. Methodology: benchmark with mock verifiers returning no verdict and verifiers returning quick PASS/FAIL, report wall-clock time and retry count distribution in P _(assumption)_
- Acceptance criteria were auto-sharpened during intake; originals: When a selected operator verifier reaches no verdict after its bounded retry, _run_review records it as no_verdict in the attempt verifier_results, emits one advisory line naming the verifier in the review event, and continues to the agentic reviewer; the attempt is neither failed nor escalated for it (a test drives _run_review with a judge that never answers and asserts the reviewer ran and no bl; A verifier that answers FAIL still short-circuits the round into a review FAIL before the agentic reviewer, exactly as today (existing tests unchanged and green); nh verifiers list shows a per-verifier count of no-verdict outcomes so a verifier that never answers is visible to the operator; docs describing verifiers state the no-verdict behaviour; structural budget re-measured with a positive control; whole uv run pytest -q -n 4 summary line from a foreground run and --strict OK in the PR body

</details>

