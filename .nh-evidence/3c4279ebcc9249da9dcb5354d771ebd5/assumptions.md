# Assumptions

_Harness-captured record for task `3c4279eb`, commit `c7f3c359ff7027e065d2366322ee748a1583bf13` — not model-authored: no_human wrote this file from the intake step's recorded questions and assumptions. It records what the gate produced; it is not a verdict of the model that wrote the code._

<details><summary>⚠️ 4 assumptions made on your behalf — verify at review</summary>

- **Q:** For 'automatic' startup update checks, does this include user-initiated 'Check for updates' actions during app startup, or only background/periodic checks that don't require user interaction? **A:** For 'automatic' startup update checks, only background/periodic checks initiated by the app without explicit user interaction qualify as automatic. User-initiated 'Check for updates' actions, even during app startup, are manual checks and should surface errors. The distinction divides on initiation: automatic=app-driven, manual=user-driven. _(assumption)_
- **Q:** When the user clicks 'Later' in Settings to dismiss the update notice: should it immediately hide the notice from the board AND prevent future automatic checks from showing it until next restart, or just hide it once per session? **A:** The Settings 'Later' action should immediately hide the update notice from the board for the remainder of the current session. It does not permanently suppress future automatic-check notices; the next app startup can surface the notice again if an update is available and the check is performed. _(assumption)_
- **Q:** Specify the exact DOM element/CSS class representing the 'top bar' (e.g., `.header`, `#navbar`, `<header>`), and confirm whether the fix must handle responsive breakpoints (mobile/tablet/desktop) differently. **A:** The exact DOM element representing the top bar cannot be determined without examining the codebase; common candidates are .header, #navbar, nav, or <header>. Responsive breakpoint handling should follow the existing pattern in the updateNotice component for mobile/tablet/desktop consistency. _(assumption)_
- **Q:** When automatic startup update checks detect an available update (succeed), should the update notice appear on the board, or should automatic checks produce no UI changes regardless of outcome? **A:** Automatic startup update checks should produce no UI changes regardless of outcome (neither success nor failure). Only manual 'Check for updates' actions initiated by the user should surface the update notice on the board, whether indicating available updates or displaying errors. _(assumption)_
- Acceptance criteria were auto-sharpened during intake; originals: Automatic startup update-check failures produce no persistent/sticky notice; a test proves a failed auto-check leaves the board notice-free while a manual check still surfaces its error; Settings 'Later' clears the board's update notice; test proves board state after Later; The notice renders in flow (no overlap of the top bar); the layout assertion fails on the old overlapping style (plant it once to show red, then fix); No behavior already landed in add2ad22 is duplicated or reverted; run the touched web/desktop test files, not the full suite

</details>

