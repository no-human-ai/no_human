# Assumptions

_Harness-captured record for task `c066fde5`, commit `c3d260723d095402471e35210c45396cae025074` — not model-authored: no_human wrote this file from the intake step's recorded questions and assumptions. It records what the gate produced; it is not a verdict of the model that wrote the code._

<details><summary>⚠️ 5 assumptions made on your behalf — verify at review</summary>

- **Q:** Is the MINOR section (guard evasion tightening and unavailable-without-latest handling) required for PR acceptance, or is it optional? **A:** The MINOR section is optional—it is not listed in the explicit acceptance criteria. The criteria name only the four MAJOR fixes, plus the test/build/citation verification gates. MINOR describes edge cases for guard tightening and the unavailable-without-latest payload edge, but since they are not in acceptance criteria and the task gates success on the MAJORs, they are descoped from PR acceptance. _(assumption)_
- **Q:** For the MAJOR-2 defer push test, should it verify only the success case (defer() returns {mode:'skipped', reason:'deferred', latest}), or also cover other defer() return values and failure modes? **A:** The test must cover both success and failure paths: assert the push happens when defer() returns {mode:'skipped', reason:'deferred', latest}, verify ordering (push after defer() ran), and assert no push when defer() returns failed. The task explicitly requests '(c) no push when defer() returns failed', so failure-mode coverage is mandatory. _(assumption)_
- **Q:** For the MAJOR-3 board-wiring lexical tests, which existing test file should they be added to, or should a new file be created? **A:** Add the five lexical tests to an existing web test file focused on board or updates functionality (most likely the file that tests updateBanner or board-update wiring, if one exists), rather than creating a new file. If no obvious home exists, create a new focused file for board-wiring mutations (e.g., updateBanner.mutations.test.js) to keep mutation detection tests together and legible. _(assumption)_
- **Q:** For the 'unavailable payload without latest' edge case, should updateBanner code be modified defensively (e.g., with a default latest), or should it be documented as a required precondition? **A:** Document the unavailable-without-latest precondition. The task flags this as MINOR (not in acceptance criteria), and defending against an edge case that callers should never trigger is lower priority than implementing the four MAJOR fixes. A comment in updateBanner stating 'unavailable payloads require latest' is sufficient. _(assumption)_
- **Q:** For the lexical guard tightening, should validation be implemented as runtime test assertions that verify no CSS overrides or className bypasses exist, or as static file/code scanning? **A:** Implement as static file/code scanning. The term 'lexical' and the constraint 'only kind possible without a DOM renderer' indicate static analysis of CSS and className expressions, not runtime assertions. Scanning should check for .nh-update-banner rules across all CSS files and inspect className string expressions for prefix bypasses like '"nh-stale-banner " +'. _(assumption)_

</details>

