# Assumptions

_Harness-captured record for task `82644133`, commit `8ffe6bbee032c4dc82f78b47e17e7ce2ff9f2d3b` — not model-authored: no_human wrote this file from the intake step's recorded questions and assumptions. It records what the gate produced; it is not a verdict of the model that wrote the code._

<details><summary>⚠️ 4 assumptions made on your behalf — verify at review</summary>

- **Q:** How should updater.mjs pass the raw error to web for display in a collapsed Details section? Should the error event payload structure include a separate `rawError` or `details` field alongside the friendly `error` message, or should raw error be handled differently? **A:** The event payload should include both a friendly `error` message string and a separate `rawError` field (or `details` field) containing the raw electron-updater error. This cleanly separates concerns: updater.mjs emits both, main.mjs forwards both, and updateNotice.js displays the friendly message above the fold with optional collapsed rendering of the raw error. _(assumption)_
- **Q:** The task specifies user messages for three error categories (HTTP 404/missing metadata, network errors, other HTTP errors). What should updatePolicy return for error types outside these categories, such as timeouts, file system errors, permission errors, or unexpected error shapes? **A:** For error types outside the three specified categories (timeouts, file system errors, permission errors, unexpected shapes), updatePolicy should return the fallback message 'Update server is temporarily unavailable', treating all unrecognized failures conservatively rather than attempting to classify them. _(assumption)_
- **Q:** How should updater.mjs maintain silent startup failure behavior described in 'the startup (non-manual) path which must remain silent on failure'? Should it suppress error event emission entirely during non-manual checks, or emit with a flag for web to conditionally suppress display? **A:** updater.mjs should emit the error event with a contextual flag (e.g., `isManualCheck: boolean` or `checkType: 'startup'|'manual'`), allowing updateNotice.js to conditionally suppress error display during startup while showing errors for manual checks. This preserves silent startup behavior while allowing updatePolicy to be invoked uniformly. _(assumption)_
- **Q:** Should the three error messages ('Release update information is unavailable', 'Check your internet connection', 'Update server is temporarily unavailable') and any fallback message be integrated into an i18n/localization system if one exists in the codebase, or hardcoded as English strings in updatePolicy.mjs? **A:** Error messages should be hardcoded as English strings in updatePolicy.mjs (or in a simple configuration object in that module), since no i18n system is mentioned in the task spec. This approach is reversible: if i18n infrastructure is added later, the strings can be moved to resource files without affecting the classification logic. _(assumption)_
- Acceptance criteria were auto-sharpened during intake; originals: a failed manual update check shows a one-sentence class-specific message; the raw error text (headers, stack) never appears above the fold - proven by a test feeding an HttpError-shaped message and asserting x-github-request-id and browser_init are absent from the primary line; updateMessage()'s existing strings, the check/download/install flow and the silent startup check are unchanged; desktop and web npm test green

</details>

