# Assumptions

_Harness-captured record for task `86b5bf3d`, commit `07e19290c7a8743ef2d82a277e7649a4d8b65d31` — not model-authored: no_human wrote this file from the intake step's recorded questions and assumptions. It records what the gate produced; it is not a verdict of the model that wrote the code._

<details><summary>⚠️ 4 assumptions made on your behalf — verify at review</summary>

- **Q:** How should the preflight determine which source files have citations pointing to them, to implement the optimization that skips --check when the diff touches no cited files? Should it parse the citation source files (docs/security.md, docs/eval.md, docs/KNOWN_ISSUES.md) and CITATION_TABLE rows directly, call a helper from reanchor_citations.py, or use another detection mechanism? **A:** Call a helper function from reanchor_citations.py that returns the set of all source files with citations pointing to them. This avoids duplicating citation-parsing logic that the script already contains and keeps the parsing rules in a single source of truth. The preflight then skips --check when the diff touches none of those files. _(assumption)_
- **Q:** How does `scripts/reanchor_citations.py --check` communicate whether discovered drift is auto-fixable versus unfixable—via exit code distinction, output text patterns (e.g., VERDICT string), or another signal? **A:** Via exit code distinction: exit 0 for no drift, non-zero exit code for fixable drift, and a different non-zero code or output text pattern (e.g., a VERDICT line) to signal unfixable drift. The preflight reads either the exit code or parses the script's stdout for the VERDICT marker to decide whether to buy a corrective round or fail immediately. _(assumption)_
- **Q:** Should the corrective round instruction message include drift details from --check output (which citations shifted, by how many lines), or only the boilerplate "run scripts/reanchor_citations.py --apply and commit"? **A:** Only the boilerplate instruction: 'Run scripts/reanchor_citations.py --apply and commit the result.' Do not parse or include drift details from --check output. This mirrors the budget preflight precedent, keeping the corrective round concise and user-owned. _(assumption)_
- **Q:** How should the preflight handle runtime subprocess errors (file not found, permission denied, script exception) that are not drift-related? Should it fail the attempt immediately, gracefully omit the preflight, or treat such errors as equivalent to unfixable drift? **A:** Fail the attempt immediately with the subprocess error text. Errors like file-not-found, permission-denied, or script exceptions indicate environmental/setup issues that prevent validation; they are not auto-fixable drift and do not warrant spending a corrective round. The coder must fix the underlying issue before the next attempt. _(assumption)_

</details>

