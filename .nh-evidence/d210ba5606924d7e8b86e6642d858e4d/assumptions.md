# Assumptions

_Harness-captured record for task `d210ba56`, commit `61f7246a8273c59f9d21fd880a886549438ba05f` — not model-authored: no_human wrote this file from the intake step's recorded questions and assumptions. It records what the gate produced; it is not a verdict of the model that wrote the code._

<details><summary>⚠️ 6 assumptions made on your behalf — verify at review</summary>

- **Q:** Should the GitHub Action be implemented as a composite action (native to the runner) or a Docker action (containerized)? **A:** Docker action. It provides a self-contained environment with all dependencies packaged, works on any runner with Docker, and does not require the host to have Python or the no_human package pre-installed. This is the simplest and safest approach for distribution to arbitrary user repositories. _(assumption)_
- **Q:** For fork PRs, should the Action (a) restrict to same-repository PRs and skip forks with a clear explanation, or (b) read diffs via the GitHub API without executing the fork's code? **A:** Restrict to same-repository PRs and skip forks with a clear explanation. The task spec explicitly warns that executing fork code under pull_request_target (which carries secrets) is a known privilege-escalation hole. Skipping forks is the secure conservative choice required by the security constraint. _(assumption)_
- **Q:** What type of cost bound should limit the reviewer run—a token budget (stop after N tokens), a file budget (review first N files), a timeout (max N seconds), or another constraint—and how should users configure it? **A:** File budget: review the first N files, configurable by the user as an action input. This is predictable, easy to understand, directly maps to reviewer work, and gives users clear control over cost. Default to a reasonable limit such as 10–20 files. _(assumption)_
- **Q:** How should the Action accept the user's credential (OAuth token or API key) and ensure only the appropriate credential path is active? Should there be separate inputs for each type, or should it infer the type from a single input? **A:** Single credential input that accepts either an OAuth token or an API key; the reviewer code auto-detects the mode. This keeps the action.yml simpler and UX cleaner. Credential scrubbing (ensuring only the active path is billed) is handled by the reviewer logic itself. _(assumption)_
- **Q:** Should the Action update an existing comment from prior runs on the same PR, or post a new comment each time? **A:** Update an existing comment from prior runs on the same PR. This is the pattern used by GitHub's own checks and avoids comment spam. Users see a single evolving summary, not a thread of identical comments. _(assumption)_
- **Q:** Is the 'tamper guard' already implemented as a callable component that returns tamper findings with file and line numbers, or does this capability need to be built? **A:** Likely callable as part of the AdversarialReviewer.review() method or as a separate integrated component, but the exact interface and whether it currently returns file-and-line citations must be confirmed by inspection. If not present, that finding-format capability will need to be added. _(assumption)_

</details>

