# Assumptions

_Harness-captured record for task `ab8f8358`, commit `5269b68f3438797e013a4e852b0e1bd57f5dc3ef` — not model-authored: no_human wrote this file from the intake step's recorded questions and assumptions. It records what the gate produced; it is not a verdict of the model that wrote the code._

> ⚠️ **Unresolved:** mechanical derived-artefact conflict resolution failed: https://github.com/no-human-ai/no_human/pull/475 step=budget

> ⚠️ **Open question:** PR https://github.com/no-human-ai/no_human/pull/475 conflicts only in derived artefact(s) (RELEASE_MANIFEST.txt, tests/test_structural_budget.py) but mechanical resolution failed at step 'budget'; detail: could not run the tests/test_structural_budget.py scanner against the merged tree: scanner module /var/folders/1r/3r0rt1jd4j1456rsg_fh4d380000gn/T/nh-derived-t7cowa1j/src/no_human/testing/structu

<details><summary>⚠️ 6 assumptions made on your behalf — verify at review</summary>

- **Q:** Should the branch name and commit SHA be added as new top-level fields to the task outcome object (e.g., 'branch' and 'commit_sha' fields), or nested within an existing 'metadata' or 'details' field? **A:** Add branch name and commit_sha as new top-level fields on the task outcome object. These represent critical success artifacts that must be immediately queryable and human-visible at the task level, not nested in a details structure. _(assumption)_
- **Q:** Should we introduce a new status value (e.g., 'PARTIAL_SUCCESS' or 'COMPLETED_WITH_ERROR') to distinguish crashes-after-commit from regular FAILED, or keep FAILED and add a distinguishing flag field? **A:** Introduce a new status value (e.g., COMPLETED_WITH_ERROR or PARTIAL_SUCCESS) distinct from FAILED. This makes the outcome directly queryable and unambiguously distinguishable without requiring downstream inspection of flag fields. _(assumption)_
- **Q:** In which module or function should the exception handler be placed to catch exceptions and record the commit info after git commit but before PR is created? (e.g., scheduler.py, the PR-opening orchestration, vcs module) **A:** Place the exception handler in scheduler.py wrapping the attempt-completion orchestration, specifically from after git commit verification through PR creation. This captures the full post-commit pre-PR window at the point where the scheduler coordinates the task lifecycle. _(assumption)_
- **Q:** What precisely defines the boundaries of the 'post-commit pre-PR window'? (Start: immediately after git commit returns, or after verification receipt is recorded? End: before push, before the PR-open API call, or before remote PR is created?) **A:** Start boundary: immediately after git commit succeeds and verification receipt is recorded. End boundary: before the PR creation API call is made. The window spans commit confirmation, any git push operations, and pre-flight checks before PR opening. _(assumption)_
- **Q:** How should the exception handler reliably obtain the commit SHA and branch name—via git commands (git rev-parse HEAD, git branch --show-current), a state variable set during the attempt phase, or another method? **A:** Use git commands in the exception handler: git rev-parse HEAD for commit SHA and git branch --show-current for branch name. These are reliable, do not depend on internal state tracking, and remain robust even if partial cleanup occurs. _(assumption)_
- **Q:** Should the exception handler validate that the branch and commit refs still exist in the local repository before recording them, or assume they are always present? How should missing refs be handled? **A:** Do not require validation; assume refs exist after a successful commit. Attempt retrieval via git commands but record whatever is obtained rather than failing if refs are missing. This ensures completed work is tracked even under partial cleanup, and surfaces anomalies to humans instead of silently losing commits. _(assumption)_

</details>

