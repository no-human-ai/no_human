# Assumptions

_Harness-captured record for task `7f1660bb`, commit `cf976c58f940b8be326273f8884833e2a2f8d175` — not model-authored: no_human wrote this file from the intake step's recorded questions and assumptions. It records what the gate produced; it is not a verdict of the model that wrote the code._

<details><summary>⚠️ 5 assumptions made on your behalf — verify at review</summary>

- **Q:** The acceptance criteria require fork detection via _is_fork_pr on pull_request events before any credential read. Should the same fork detection also apply when the event type is workflow_run, or should workflow_run events bypass fork checks entirely? **A:** Yes, fork detection should apply to workflow_run events. The security model requires checking if a PR originates from a fork regardless of the event type. The fact that the workflow executes on the default branch does not change the security boundary around reviewing forks; bypassing fork checks for workflow_run would create an unintended privilege escalation path. _(assumption)_
- **Q:** The acceptance criteria specify exit code 2 for REST API non-200 responses. When the tamper guard cannot run because a checked-out tree is unavailable, should the function also return exit 2, or should it return a different code (e.g., 0)? **A:** Exit code 2. When the tamper guard cannot run due to unavailable tree, this represents a failure state equivalent to API errors (non-200 responses). FAIL CLOSED principle requires exit 2, not 0 or success, because the review gate has not completed its check. The comment must state this explicitly without implying the check ran. _(assumption)_
- **Q:** The task states 'when the tamper guard cannot run because there is no checked-out tree' but does not define how to detect this condition. Should the implementation check for a .git directory, the presence of specific repository files, an environment variable, or use a different method? **A:** Check for the existence of a .git directory. This is the canonical marker of a git repository checkout and the standard way to detect whether a local repository tree is available to the review process. _(assumption)_
- **Q:** The acceptance criteria require the comment to 'state plainly' tamper guard unavailability and contain 'the stated sentence', but the exact required phrasing is not provided. What is the specific sentence or phrase that must appear in the comment when tamper guard cannot run? **A:** A sentence such as: 'Tamper guard check could not run: no local repository tree is available.' The phrasing must explicitly state the unavailability and the reason without implying the check executed. Synonyms stating the same fact are acceptable as long as they plainly communicate that the tamper check did not run. _(assumption)_
- **Q:** How should run.py extract the pull request number and head commit SHA from the workflow_run GitHub Actions event object? Which fields in the workflow_run event payload should be used—e.g., from a pull_requests array if present, or from a canonical PR/SHA field on the root workflow_run object? **A:** Extract from the workflow_run event payload's pull_requests array: PR number from pull_requests[0].number and head commit SHA from pull_requests[0].head.sha. The workflow_run context always includes an associated pull_requests array when triggered by a pull_request workflow event. _(assumption)_

</details>

