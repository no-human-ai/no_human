# Assumptions

_Harness-captured record for task `284ffec7`, commit `04533e041c6152b2af9716f2c4694ce1dfd78e13` — not model-authored: no_human wrote this file from the intake step's recorded questions and assumptions. It records what the gate produced; it is not a verdict of the model that wrote the code._

<details><summary>⚠️ 3 assumptions made on your behalf — verify at review</summary>

- **Q:** The task specifies 'cap the detail length like other capped fields' but doesn't state the limit or which fields to reference. What is the character limit for the detail field, and which existing capped fields or capping pattern should serve as the template? **A:** Without access to the specific codebase fields, a conservative standard for error/detail fields is 256–512 characters. Common templates in logging systems use 256 for succinctness or 512 for richer context. Recommend checking existing fields like timeout messages, import errors, or resolution step descriptions in the blocker/resolution modules for the pattern already in use. A senior engineer woul _(assumption)_
- **Q:** The task says to update 'wherever a failed DerivedResolution produces the escalated_pr_conflict event' and mentions 'budget or derived path,' but doesn't enumerate all code locations. What are the specific functions, code paths, or conditions in wake.py that need modification—are there only two (budget and derived), or are there others? **A:** The task cites 'budget or derived path' and explicitly names resolve_derived_conflict as the source of the DerivedResolution. At minimum, two code paths must be modified: one handling budget resolution failure and one handling derived resolution failure. The task also mentions 'the escalated_pr_conflict path (src/no_human/blockers/wake.py, the branch that returns after resolve_derived_conflict fai _(assumption)_
- **Q:** When appending detail to the blocker's 'stored evidence/question,' what is the exact data structure or field being modified, and what format should be used—should detail appear on a new line, with a separator (colon, pipe, etc.), or directly concatenated to the existing step text? **A:** The task describes appending detail to 'the blocker's stored evidence/question', indicating a single text field already containing the step description (e.g., 'step budget'). Format should match the current pattern: if it already reads 'step: <step>', append detail inline (e.g., 'step: budget, detail: <text>'), or if lines are separate, add it on a new line. Use a separator consistent with the exi _(assumption)_

</details>

