# Assumptions

_Harness-captured record for task `c8cebc3e`, commit `cfc3c68a9151f481780e72cae3e0269bff4787cd` — not model-authored: no_human wrote this file from the intake step's recorded questions and assumptions. It records what the gate produced; it is not a verdict of the model that wrote the code._

<details><summary>⚠️ 5 assumptions made on your behalf — verify at review</summary>

- **Q:** How should the app obtain the hosted registration endpoint URL in production (environment variable, config file, hardcoded, service discovery, or other)? Does the endpoint require authentication (API key, bearer token, or other), and if so, how should the app obtain the credentials? **A:** HUMAN-GATED: not self-answerable
- **Q:** Where should the app determine the "platform" value to forward in the outbound POST? Should it be extracted from the User-Agent header, passed in the request body, derived from local device/OS metadata, or use a preconfigured/hardcoded value? **A:** The app should determine platform from local device/OS metadata detected at runtime during app initialization, not from User-Agent headers (unreliable for desktop apps) or request body (redundant when the app knows its own platform). This ensures accurate, consistent platform identification across invocations. _(assumption)_
- **Q:** Should the privacy disclosure on the Email step require user acknowledgment/gating (blocking form submission until confirmed), or should it be purely informational text that does not block submission? **A:** The privacy disclosure should be purely informational text that does not block form submission. Regulatory compliance requires disclosure but not affirmative consent for this category of telemetry. The operator can add submission-blocking acknowledgment logic later if their specific privacy policy requires it; this keeps the implementation scope focused and legal wording operator-owned as stated. _(assumption)_
- **Q:** What timeout threshold should the app use for the outbound POST to the registration endpoint (e.g., 5 seconds, 10 seconds, 30 seconds)? And what status strings should appear in the /api/onboarding/email response body (e.g., {"status": "ok"} on success vs. {"status": "stored_locally_only"} on forward failure/timeout)? **A:** Use a 10-second timeout for the outbound POST (standard for user-facing sync operations; 5 seconds too aggressive for desktop network reliability, 30 seconds too long for UX). Response schema: {"status": "ok"} on success, {"status": "stored_locally_only"} on forward failure or timeout, using closed-vocabulary strings to avoid leaking PII in error detail. _(assumption)_
- **Q:** Should the app reuse the existing waitlist endpoint (which accepts POST {email, plan, source} and already handles desktop-* plans) for onboarding email registration, or define a new dedicated registration endpoint? **A:** Reuse the existing waitlist endpoint. It already accepts POST {email, plan, source} and handles desktop-* plans. Onboarding can pass source="onboarding" to distinguish signups. This avoids a new cloud-side endpoint implementation and follows DRY; the no_human-cloud repo team does not need to add a dedicated registration endpoint. _(assumption)_

</details>

