# Assumptions

_Harness-captured record for task `ec3fbb0e`, commit `5add0aaa699d62c1041b7d3afa13b5b2d5c674cd` — not model-authored: no_human wrote this file from the intake step's recorded questions and assumptions. It records what the gate produced; it is not a verdict of the model that wrote the code._

<details><summary>⚠️ 3 assumptions made on your behalf — verify at review</summary>

- **Q:** The task requires build_cmd execution to be 'consistent with how start_cmd is launched today' (shell=False-style argv split versus bash -lc), but does not specify which mechanism start_cmd currently uses. Which execution method should build_cmd adopt—or shall I trace the codebase to verify? **A:** build_cmd should adopt shell=False-style argv split (using shlex.split or manual tokenization), matching the most robust and consistent subprocess execution pattern. If start_cmd in the codebase currently uses shell=True with bash -lc for complex commands, build_cmd should mirror that; otherwise, argv-split is safer and more portable. The task description's mention of both patterns suggests checki _(assumption)_
- **Q:** When deciding whether to run build_cmd, the walk checks 'nothing already answering at the manifest base_url.' What specific method should detect that readiness (e.g., TCP connection attempt, HTTP GET to base_url, or reuse of existing dev_server readiness logic), and what timeout should that check use? **A:** Server readiness should be detected via a single HTTP GET request to the base_url with a short timeout (2–3 seconds). This is more reliable than TCP-only probes because it confirms the server is actually responding to HTTP requests, not just listening. If ui_evidence.py already has an existing dev_server readiness probe (e.g., in a boot-loop or fixture setup), reuse that logic and timeout constant _(assumption)_
- **Q:** When build_cmd fails (nonzero exit or timeout), the task requires logging 'exit code and last ~10 log lines' to the ADVISORY log 'using the existing detail-is-log-only convention in ui_evidence.py.' Can you confirm which log level/channel and format that convention uses, or should I extract the pattern from the existing ui_evidence.py code? **A:** Log build failures to the existing logger at the ERROR or WARNING level, capturing the exit code and last ~10 lines of command output (stdout+stderr combined or separately). The rendered skip-section message should read approximately: 'UI evidence skipped: build command failed (exit code <N>) — last log lines: <snippet>'. This follows the pattern of disclosing the reason in the public section whil _(assumption)_

</details>

